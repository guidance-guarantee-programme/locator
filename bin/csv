#!/usr/bin/env ruby

require 'bundler/setup'

require 'csv'
require 'json'

json = JSON.parse(File.read(ARGV.first))

headers = json.first.keys + json.first.keys.map { |k| "new_#{k}" }

csv = CSV.generate do |csv|
  csv << headers
  json.each { |record| csv << record.values }
end

puts csv
